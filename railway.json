{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf node_modules/@prisma && npm install --ignore-scripts && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "npx prisma db push && node dist/src/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/auth/setup",
    "healthcheckTimeout": 300
  }
}
